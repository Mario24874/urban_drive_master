import React from 'react';
import Landing from '../components/landing/Landing';

/** Public landing page (Kinetic Dark / Neon Mobility redesign). */
const HomePage: React.FC = () => {
  return <Landing />;
};

export default HomePage;
